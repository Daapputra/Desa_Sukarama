{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/muhammadrahardianbaihaqi/Projects/Desa_Sukarama\" 2>/dev/null || exit 0; command -v git >/dev/null 2>&1 || exit 0; git rev-parse --is-inside-work-tree >/dev/null 2>&1 || exit 0; changes=$(git status --porcelain -- apps/web 2>/dev/null); if [ -n \"$changes\" ]; then printf '{\"systemMessage\": \"apps/web punya perubahan yang belum di-build ke Docker. Jika perubahan ini perlu tampil di container web (bukan cuma di dev server), jalankan: docker compose up --build -d web\"}'; fi; exit 0",
            "statusMessage": "Mengecek perubahan apps/web yang belum di-build ke Docker..."
          }
        ]
      }
    ]
  }
}
